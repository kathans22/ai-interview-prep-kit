/**
 * PracticePage.jsx — skeleton for the practice run.
 *
 * Decides: nothing yet. Heading only.
 *
 * Does NOT decide: how confidence is recorded. Ratings live beside the kit rather than
 * inside it and take no revision, so practising in one tab can never conflict with a
 * regeneration finishing in another.
 */

export default function PracticePage() {
  return (
    <section>
      <h1 className="text-2xl font-semibold tracking-tight text-slate-900">Practice</h1>
      <p className="mt-2 text-sm text-slate-600">The practice run arrives with the kit hooks.</p>
    </section>
  );
}
